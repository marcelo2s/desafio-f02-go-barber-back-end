class Balance {
  income: number;

  outcome: number;

  total: number;

  constructor(income: number, outcome: number, total: number) {
    this.income = income;
    this.outcome = outcome;
    this.total = total;
  }
}

export default Balance;